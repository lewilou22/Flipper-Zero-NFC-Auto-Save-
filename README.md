For Momentum firmware 

Install .fap to APPS folder 

Auto Saves any scanned NFC tags into the labs folder

Uses the first scanned tag and creates a seed file to scan the same type of band faster. (after a successful scan of the first tag type.)
